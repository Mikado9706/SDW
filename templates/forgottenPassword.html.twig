{% extends 'base.html.twig' %}

{% block body %}
<main>
    <div class="container">
        <div class="row blue">
            <div class="col l12">
                <p>forgottenPassword</p>
                
                {{ form_start(form) }}
                {{ form_widget(form.email) }}
                {{ form_widget(form.send) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</main>
{% endblock %}
