{% extends 'base.html.twig' %}

{% block body %}
<main>
    <div class="container">
        <div class="row blue">
            <div class="col l12 s12">
                <p>Réinitialiser votre mot de passe</p>
                {{ form_start(form) }}
                <div class="input-field subSelect col l6 offset-l3 s8 offset-s2">
                    <div class="row">
                        {{ form_widget(form.password.first) }}
                    </div>
                    <div class="row">
                        {{ form_widget(form.password.second) }}
                    </div>
                </div>
                <div class="col l12 search-section">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col l12">
                                <div class="search-button">
                                    {{ form_widget(form.reset) }}
                                </div>
                            </div>
                        </div>                
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</main>
{% endblock %}
